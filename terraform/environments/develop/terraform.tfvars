# This file is intentionally safe to commit.
# For local runs, replace these examples or create a separate untracked tfvars file.
project_name = "dam-platform"
environment  = "develop"

database_name   = "dam"
database_schema = "public"
minio_bucket    = "assets"
