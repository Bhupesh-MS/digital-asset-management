# @dam/web

Frontend Web Application for the DAM platform.

Built using React, Vite, TailwindCSS, and Zustand.

## Setup

1. Ensure `VITE_API_BASE_URL` is set in the `.env` file to point to the backend API.
2. Run `pnpm install`.
3. Run `pnpm run dev` to start the Vite development server.
