# @dam/shared-types

Shared TypeScript types and interfaces for the DAM platform.

This package contains types that are shared between the frontend (web), backend API, and worker services, ensuring end-to-end type safety.
