# @dam/asset-processing

Asset processing utilities for the DAM platform.

Contains tools and functions to process media files (e.g., generating thumbnails using FFmpeg, extracting metadata).

## Requirements

Ensure `ffmpeg` is installed on the system where this code runs.
